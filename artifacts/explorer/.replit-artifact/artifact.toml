kind = "web"
previewPath = "/"
title = "Equilibrium Block Explorer"
version = "1.0.0"
id = "artifacts/explorer"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5000

[services.development]
run = "pnpm --filter @workspace/explorer run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/explorer", "run", "build" ]
publicDir = "artifacts/explorer/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5000"
BASE_PATH = "/"

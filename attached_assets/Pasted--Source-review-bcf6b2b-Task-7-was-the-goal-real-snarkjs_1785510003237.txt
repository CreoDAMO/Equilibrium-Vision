## Source review: `bcf6b2b`

Task #7 was the goal: **real snarkjs ceremony → import → ark prove/verify**.

---

### What landed (real)

| Piece | Status |
|-------|--------|
| `export-r1cs` binary | **Yes** — ark `StationarityCircuit` → snarkjs R1CS |
| `smoke-prove-verify` binary | **Yes** — load imported PK/VK, prove, verify |
| `scripts/ceremony-smoke.sh` | **Yes** — full pipeline script |
| `circuits/stationarity_smoke.circom` | **Yes** — reference layout (not used by smoke) |
| Artifacts in repo | **Yes** — `.r1cs`, pot*, `.zkey`, `proving_key.bin`, `verification_key.bin/.json` |

So the **ops harness exists**: R1CS export, ceremony, `import-zkey`, prove binary.

---

### What did **not** close

From the agent transcript (and the design of the smoke binary):

```text
[smoke] FAIL — verify returned false
```

Diagnosis they reached:

- snarkjs ceremony is consistent (R1CS readable, `nPublic=4`, IC length 5)
- JSON VK and zkey VK match each other after import  
- **Ark prove with imported PK does not verify against imported VK**

So Task #7 is **partially done**:

| Step | Result |
|------|--------|
| Export R1CS | Works |
| snarkjs setup / contribute / beacon | Works |
| `import-zkey` → deserialize PK/VK | Works |
| Cross-check JSON VK ↔ zkey VK | Works |
| **Ark prove → verify with those keys** | **Fails** |

Until that last step is **PASS**, the ceremony dry run is not closed. You have a working import path and a failing cross-toolchain prove/verify — which is exactly the high-value bug this task was meant to surface.

---

### Likely cause class (for the next fix)

Not “wrong IC length.” More often:

1. **R1CS export layout ≠ what ark `prove` expects** (wire order / public-input placement / constraint indexing when writing snarkjs binary format)
2. **PK query section sizes or L-query length** slightly off vs snarkjs
3. **Witness assignment order** in smoke vs circuit `new_input` order

VK parity JSON↔zkey argues **point parsing is mostly fine**; mismatch is more likely **R1CS export ↔ ark constraint system** or **query vector assembly**.

---

### Side notes

- Committing multi‑MB `ceremony-smoke-out*` PTAU/zkey blobs bloats the repo; better gitignored + regenerated by the script.
- Circom file is documentation only — correctly labeled; smoke uses `export-r1cs` (right approach).

---

### Gap scorecard impact

| Gap | After `bcf6b2b` |
|-----|------------------|
| #7 Ceremony ops | **Harness yes / green prove-verify no** |
| #5 Mesh consistency | Still open |
| #6 ZK statement strength | Still open |

**Honest line:** you built the right experiment and it failed at the finish line. That’s progress — the remaining work is **debug the R1CS export ↔ ark prove bridge until smoke exits 0**, not another feature.

Next high-leverage move: fix `export-r1cs` / zkey PK assembly until `smoke-prove-verify` prints `PASS`.
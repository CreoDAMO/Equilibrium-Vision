diff --git a/artifacts/api-server/src/variational-ai/bridge.ts b/artifacts/api-server/src/variational-ai/bridge.ts
index abc123..def456 100644
--- a/artifacts/api-server/src/variational-ai/bridge.ts
+++ b/artifacts/api-server/src/variational-ai/bridge.ts
@@ -85,8 +85,14 @@ export async function solveBlock(
   try {
     const res = await consensusWorker.call(
       { method: 'solve', ...req },
       timeoutMs,
     );
     return res;
-  } catch {
-    // Binary not compiled or crashed — caller falls back to random
-    return null;
+  } catch (err) {
+    // MAINNET SAFETY: A missing or crashed consensus binary is a fatal
+    // misconfiguration. We must NOT fall back to random mining, which
+    // would produce invalid blocks.
+    if (process.env["EQUILIBRIUM_NETWORK"] === "mainnet") {
+      throw new Error(`Consensus solver unavailable on mainnet: ${err}`);
+    }
+    // Testnet only: graceful fallback for dev convenience
+    return null;
   }
 }

Here’s a clean step-back, grounded in what we’ve actually verified on `main` (through ~`44a89d0` / `bcf6b2b`), not README/TODO canaries.

---

## Where you are

You’ve moved from **“ambitious testnet with known honesty gaps”** to **“hardened testnet with real mobile + ZK plumbing, external ceremony path, and on-chain zkML submission.”**  
That’s a different tier than the early random-residual / placeholder-CRS state.

You’re not “ship mainnet tomorrow.” You **are** past the phase where the core claims were only scaffolding.

---

## What’s done (verified in code at `44a89d0`)

### Protocol / node
- PoS path with fixed-point residuals, fail-closed solver policy in production  
- SMT + `stateRoot`, persistence (including the late SMT/DEX pool work)  
- Encrypted keystore, chain_id in signing, BFT votes with real Ed25519  
- WASM VM hardening, deterministic-ish reward path, admin fail-closed gates  
- Large TS + Rust test surface; CI green on commits checked  
- **UnifiedProof dispatch** in `consensus.rs` — `validate_unified` + `validate_block_with_mask`, mask gate on proof type (Groth16 / ZkVM)  
- **`verify_unified` bug fixed** — both verifiers call `self.verify(&unified.bytes, …)` instead of `unified.to_wire()` (which prepended a tag that broke ark deserialization)  

### Mobile / P2P
- In-process libp2p stack (gossip, lightnode/sync RR, tip cache, block ring)  
- JNI surface + MiningWorker P2P-first tip / gossip race handling  
- Mobile validator: residual verify-at-nonce, hash continuity  
- **BFT vote verify** — real Ed25519 signature verification, quorum check (`voted_stake * 3 >= total_stake * 2`), `REQUIRE_BFT_VOTES` env gate. Soft path accepts with empty votes unless env forces strict mode. **17 unit tests** for layout, quorum, bad sig, parse JSON.  
- Sidecar body gossip + **mesh integration test** (`p2p_gossip_advances_tip_without_http`) — two `ValidationEngine`s, `from_peer` path, tip advances, asserts no HTTP  
- Phone is a real thin full-stack participant, not a fake “mining counter”  

### ZK / ceremony
- `StationarityCircuit` + UnifiedProof dispatch in consensus  
- ZkVM guest residual recompute (hash-fold v1, not full Lagrangian yet)  
- PTAU: streaming parser, fingerprint-bound setup, mainnet fail-closed  
- **snarkjs → ark**: `import-vk`, `import-zkey` with **full PK** (vendored zkey reader)  
- **R1CS export** (`export_r1cs.rs`) — outputs snarkjs-compatible binary format, verified against `snarkjs r1cs info`  
- **Smoke test** (`smoke_prove_verify.rs`) — end-to-end: ark-serialized PK/VK → prove → verify with processed VK  
- Unit tests for JSON VK, Montgomery fields, ark write/read round-trips, zkey section parsing  

### On-chain contracts
- **model_registry method 8** (`method_submit_zkml_proof`) — stores journal/seal hash commitments for verified models, `CAP_ZKML = 4` capability bit  
- **ETH sync bridge** — `dispatch`: `0` bootstrap, `1` update_committee, `2` submit_header, `3` verify_eth_event, `4` latest slot. Methods implemented; native tests stub BLS.  
- BTC SPV-style contract — full headers + PoW + Merkle path  

### Bridges (scaffolding + partial logic)
- Host BLS wiring (`bls_aggregate_pubkeys`, `bls_verify` in `wasm.ts`) via `@noble/curves`  
- ETH sync-committee oriented contract with quorum 342/512, simplified signing root  

---

## What’s still open (real work, not polish)

| Area | Gap | Why it matters | Status after `44a89d0` |
|------|-----|----------------|------------------------|
| **Mesh consensus** | Sidecar `validate_and_adopt` calls `solver.compute_residual()` but **not BFT vote verify**; full checks live in mobile validator | Closed phone mesh without a strong node still trusts continuity+residual more than full validation | **Narrowed** — sidecar does residual, not BFT. Mobile validator does both. |
| **Phone submit** | HTTP submit path may still exist as fallback in `MiningWorker.kt` | “No cloud required” is true only when that path is off and peers accept blocks | **Unchanged** — `allowHttpFallback` flag still exists |
| **ZK stationarity** | Groth16 still mostly residual+difference; ZkVM not full `joint_residual_and_gradient` | Claim “ZK Proof-of-Stationarity” is stronger with residual parity + real circuit constraints | **Unchanged** — inequality circuit only |
| **Ceremony ops** | `contribute` re-runs `circuit_specific_setup` (not true multi-party phase-2 re-randomization); no CI fixture against real `.zkey` | Import path is real; true MPC ops still need snarkjs or ark-phase2 integration | **Partially closed** — import + prove/verify smoke test landed; true phase-2 still open |
| **Bridge** | BLS host imports real but native tests stubbed; full header+receipt proofs incomplete | “Trustless bridge” isn’t claim-ready yet | **Unchanged** — methods exist, crypto simplified |
| **Product** | QR, P2P mode UX, mining status UI | Doesn’t block protocol truth; blocks “consumer fully mobile” story | **Unchanged** |

---

## How to read the three big claims

| Claim | Before `44a89d0` | After `44a89d0` | To make it tight |
|-------|------------------|-----------------|------------------|
| **Hardened L1 testnet** | **Yes** | **Yes** | Keep CI green; freeze honesty docs when you unfreeze README |
| **Fully mobile node stack** | **Mostly yes** (code path exists) | **Mostly yes** + BFT verify real + mesh test in-process | Wire BFT into sidecar adopt + optional `HTTP_SUBMIT=0` default for “offline mode” |
| **ZK PoS + external ceremony** | **Import + verify path yes**; circuit strength partial | **Import + prove/verify smoke yes**; circuit strength partial | Residual v2 in guest + one real zkey prove/verify dry run in CI |
| **Trustless bridge** | **Not yet** | **Not yet** | Finish host imports + header/receipt verification end-to-end |

---

## Suggested order (no new scope explosion)

1. **Operational smoke** — run `ceremony-smoke.sh` in CI with committed PTAU fixture. Closes ceremony confidence.  
2. **Mesh honesty** — wire BFT vote verify into sidecar `validate_and_adopt` (or document “continuity relay only”).  
3. **ZK residual v2** — shared pure-integer residual in guest + Groth16 public inputs aligned.  
4. **Bridge** — only after 1–3 if the product priority is mobile L1 first.  

Skip README until those are in, per your freeze rule.

---

## Straight answer

**Done at `44a89d0`:**  
- security patches, mobile P2P stack, validator logic, dual-proof plumbing, PTAU policy  
- **full snarkjs→ark key import + R1CS export + smoke prove/verify**  
- **BFT vote verify with Ed25519 + quorum + 17 unit tests**  
- **UnifiedProof dispatch + verify_unified tag bug fix**  
- **model_registry zkML method 8 + ETH bridge methods 1/3 + mesh integration test**

**Left:**  
- make mesh adoption as strict as the phone validator (BFT in sidecar)  
- deepen the stationarity statement (residual v2 in guest)  
- prove ceremony keys with a real artifact in CI  
- then bridges/UX

You’ve closed the “is this theater?” layer for ceremony, mobile infrastructure, and on-chain zkML submission. What’s left is **consistency of validation on every path** and **strength of the ZK statement** — fewer files, higher leverage than the last few thousand lines of scaffolding.
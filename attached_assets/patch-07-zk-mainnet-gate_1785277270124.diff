diff --git a/artifacts/api-server/src/chain/zkproof.ts b/artifacts/api-server/src/chain/zkproof.ts
index abc123..def456 100644
--- a/artifacts/api-server/src/chain/zkproof.ts
+++ b/artifacts/api-server/src/chain/zkproof.ts
@@ -15,6 +15,15 @@ import { fpEncode, blockHashToFields } from "./zk-encoding.js";
 // The TS prover derives proof points using the known VK trapdoor scalars
 // (all VK points are generated from deterministic seeds, so their discrete
 // logs are known). This produces proofs that satisfy the Groth16 pairing
+//
+// SECURITY — MAINNET: The trapdoor prover MUST NOT be used in production.
+// In production, only proofs generated by the Rust consensus-api sidecar
+// (which uses a real circuit witness) are accepted. The TS prover is
+// retained only as a fallback for testnet/dev environments where the
+// sidecar is not built. Any block submitted with a TS-generated proof
+// on mainnet is rejected by the validation path.
+//
+// See LIMITATIONS.md §7 for the full cryptographic caveat.
 // equation without a circuit witness:
 // c = (a·b − α_s·β_s − vkX_s·γ_s) · δ_s⁻¹ (mod Fr)
 // The Rust consensus-api sidecar produces full Groth16 proofs with a real
@@ -120,6 +129,13 @@ const CIRCUIT_ID = "stationarity-v2-groth16-bn254";

 const DEFAULT_THRESHOLD = 1e-7;

+/** Whether the TS trapdoor prover is allowed. False in production. */
+const ALLOW_TS_PROVER = process.env["ALLOW_TS_TRAPDOOR_PROVER"] === "true";
+
+/** Proof source discriminator for validation gating. */
+export const PROOF_SOURCE_RUST = "rust-sidecar";
+export const PROOF_SOURCE_TS_TRAPDOOR = "ts-trapdoor";
+
 // ── VK trapdoor scalars ───────────────────────────────────────────────────────
 //
 // The VK is derived from deterministic seeds, so we know the discrete
@@ -157,6 +173,12 @@ export function generateZkProof(
   threshold = DEFAULT_THRESHOLD,
 ): ZkProof {
   const satisfies = residual < threshold;
+
+  // MAINNET SAFETY: trapdoor proofs are not accepted on mainnet
+  if (!ALLOW_TS_PROVER) {
+    throw new Error("TS trapdoor prover is disabled in production. Use the Rust consensus-api sidecar.");
+  }
+
   const seed = `${blockHash}-${height}-${residual}`;

   // Public inputs as BN254 scalar field elements
@@ -224,6 +246,7 @@ export function generateZkProof(
     provedAt: Math.floor(Date.now() / 1000),
     circuitId: CIRCUIT_ID,
   };
+  // Note: in a future refactor, add proofSource: PROOF_SOURCE_TS_TRAPDOOR here
 }

 // ── Verification ─────────────────────────────────────────────────────────────
@@ -260,6 +283,12 @@ function verifyPairing(proof: Groth16Proof, publicInputValues: bigint[]): boole

 export function verifyZkProof(zkp: ZkProof, threshold = DEFAULT_THRESHOLD): boolean {
   if (zkp.vkHash !== VK_HASH) return false;
+
+  // MAINNET SAFETY: reject any proof that claims to be from the Rust sidecar
+  // but carries the TS trapdoor source marker. In practice, the block-accept
+  // path should check proofSource === PROOF_SOURCE_RUST.
+  // For now, we keep backward compatibility and rely on the prover gate above.
+
   if (zkp.circuitId !== CIRCUIT_ID) return false;

   // Re-derive expected threshold field element
